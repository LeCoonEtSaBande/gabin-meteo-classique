# Collecte Open-Meteo — 2 fois par jour, heures Europe/Paris.
# Le cron GitHub ne part que depuis la branche par défaut (main) :
# ce fichier doit aussi exister sur main. Le job checkout collecte-api-meteo.
name: Collecte Open-Meteo

on:
  schedule:
    # 06h / 19h CEST (heure d'été)
    - cron: "0 4,17 * * *"
    # 06h / 19h CET (heure d'hiver)
    - cron: "0 5,18 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Ignorer le filtre horaire 6h / 19h"
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: collecte-api-meteo

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Installer les dépendances
        run: pip install -r requirements.txt

      - name: Extraire les prévisions
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.force }}" = "true" ]; then
            python src/collecte/run.py --force
          else
            python src/collecte/run.py
          fi

      - name: Committer les données
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/raw
          if git diff --cached --quiet; then
            echo "Aucune donnée nouvelle à committer."
            exit 0
          fi
          git commit -m "Collecte Open-Meteo $(date -u +%Y-%m-%dT%H:%MZ)"
          git push origin HEAD:collecte-api-meteo
