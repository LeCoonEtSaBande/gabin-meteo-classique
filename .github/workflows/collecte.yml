# Collecte Open-Meteo — 2 fois par jour (cron UTC, :15 pour éviter le pic).
# En heure d'été : 6h15 / 19h15 Europe/Paris.
# En heure d'hiver : 5h15 / 18h15 (un décalage d'une heure est accepté).
# Le cron GitHub ne part que depuis la branche par défaut (main) :
# ce fichier doit aussi exister sur main. Le job checkout collecte-api-meteo.
name: Collecte Open-Meteo

"on":
  schedule:
    - cron: "15 4,17 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Ignorer le filtre des créneaux déjà collectés"
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: "1"
    steps:
      - uses: actions/checkout@v7
        with:
          ref: collecte-api-meteo

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Installer les dépendances
        run: pip install -r requirements.txt

      - name: Extraire les prévisions
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.force }}" = "true" ]; then
            python -u src/collecte/run.py --force
          else
            python -u src/collecte/run.py
          fi

      - name: Committer les données
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/raw
          if git diff --cached --quiet; then
            echo "Aucune donnée nouvelle à committer."
            exit 0
          fi
          git commit -m "Collecte Open-Meteo $(date -u +%Y-%m-%dT%H:%MZ)"
          git push origin HEAD:collecte-api-meteo
